# Conformance reports

One JSON report per endpoint and model, as `aralo conformance --report`
writes it. `make compatibility` turns them into `docs/compatibility.md`; see
[`../README.md`](../README.md).
